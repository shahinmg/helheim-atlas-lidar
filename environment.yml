name: pdal_env
channels:
  - conda-forge
  - defaults
dependencies:
  # C++ build toolchain
  - compilers
  - cmake
  - ninja
  - eigen
  # PDAL + GDAL (core data I/O)
  - pdal
  - python-pdal
  - gdal
  # Python utilities
  - python=3.13
  - numpy
  - pip
  - pip:
      - pdal-plugins
